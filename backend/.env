question_json_path = 'path to where the questions.json for llm finetuning is stored'

video_model_path =  'path to where the YOLO model is stored'

llm_file_path =  'path to where the finetuned llm is store'

image_store_path = 'path to the image in the backend/app/img/result.jpg floder'

